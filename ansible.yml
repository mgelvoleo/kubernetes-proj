- hosts: all
  become: true
  tasks: 
    
     - name: create new deployment
       command: kubectl apply -f /home/ansible/cloud-cv-pipeline/Deployment.yml
     - name: create new service
       command: kubectl apply -f /home/ansible/cloud-cv-pipeline/Service.yml